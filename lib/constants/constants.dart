import 'package:flutter/material.dart';

// Define constant colors used throughout the application
const Color bgColor = Color(0xFF242C3B); // Background color of the app (dark blue-gray)
const Color seeBlue = Color(0xFF37B6E9); // A light blue color, possibly used for buttons or highlights
const Color violet = Color(0xFF4B4CED); // Violet color, potentially for accents or branding
const Color grey = Color(0xFF353F54); // Dark grey color, used for background or text
const Color black = Color(0xFF222834); // Dark color for backgrounds or other elements

// Define the base URL for API requests
const String baseUrl = "https://api.escuelajs.co/api/v1"; // URL to the API
